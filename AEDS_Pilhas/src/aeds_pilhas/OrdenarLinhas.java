/*
 *Escreva um programa que leia linhas de texto da entrada padrão (ignore as linhas vazias) e 
* coloque as linhas em ordem alfabética.  As linhas de texto têm comprimento arbitrário e o número 
* de linhas é arbitrário.  Sugestões: Use o método readLine da classe StdIn de SW.  
* Use redirecionamento da entrada para ler os dados de um arquivo (= file) e não do teclado.  
* Construa um vetor de strings usando redimensionamento.  Use o método java.util.Arrays.sort() para 
* colocar o vetor em ordem lexicográfica. 
* Depois, repita os testes usando Insertion.sort(), Merge.sort(), e Quick.sort().
 */


package aeds_pilhas;

import java.util.Scanner;

public class OrdenarLinhas {
    public static void main(String[] args) {
        String[] linhas = new String[2];
        int contador = 0;
        
        Scanner in = new Scanner(System.in);
        
    }
    
}
